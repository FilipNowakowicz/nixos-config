# One path/glob per line for intentional false positives in the plaintext secret hook.
# Keep entries narrow and justified in the commit/PR description.

# SHA1 htpasswd entry for admin:test used in the homeserver-gcp smoke test.
# Not a real credential — only verifies nginx parses the file and returns 401.
tests/nixos/homeserver-gcp-smoke.nix

# Restore runbooks reference /run/secrets/* paths as command arguments, not real secrets.
.claude/homeserver-gcp/restore-vaultwarden.md
.claude/homeserver-gcp/restore-adguard.md
