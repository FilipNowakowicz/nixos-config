result
result-*
.worktrees
.pre-commit-config.yaml
__pycache__/
*.py[cod]

# Claude Code local data (worktrees, session state) — not shared.
/.claude/
!/.claude/settings.json

# Large VM disk images — installed to /var/lib/libvirt/images/, never committed.
*.qcow2
*.qcow2.xz
